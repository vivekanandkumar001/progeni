# Progeni - Flask Migration

Run locally:
1. copy .env (local)
2. python -m venv venv
3. source venv/Scripts/activate (Windows) or source venv/bin/activate
4. pip install -r requirements.txt
5. python app.py
Open http://127.0.0.1:5000
